{% extends 'base.html.twig' %}

{% block title %}Edit Section{% endblock %}

{% block body %}
    <h1>Edit Section</h1>

    {{ include('webapp_section/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('webapp_section_index') }}">back to list</a>

    {{ include('webapp_section/_delete_form.html.twig') }}
{% endblock %}
