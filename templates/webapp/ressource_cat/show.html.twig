{% extends 'base.html.twig' %}

{% block title %}RessourceCat{% endblock %}

{% block body %}
    <h1>RessourceCat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ressource_cat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ressource_cat.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ ressource_cat.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('webapp_ressource_cat_index') }}">back to list</a>

    <a href="{{ path('webapp_ressource_cat_edit', {'id': ressource_cat.id}) }}">edit</a>

    {{ include('webapp_ressource_cat/_delete_form.html.twig') }}
{% endblock %}
