{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ comment.CreateAt ? comment.CreateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('webapp_comment_index') }}">back to list</a>

    <a href="{{ path('webapp_comment_edit', {'id': comment.id}) }}">edit</a>

    {{ include('webapp_comment/_delete_form.html.twig') }}
{% endblock %}
