{% for ressource in ressources %}
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-3">

                </div>
                <div class="col-9">
                    <h3>{{ ressource.name }}</h3>
                    {{ ressource.content|raw }}
                </div>
            </div>
        </div>
        <div class="card-footer">
            <div class="row">
                <div class="col-12">
                    {{ ressource.category }}
                </div>
            </div>
        </div>
    </div>
{% else %}
pas de ressource.
{% endfor %}