{% extends 'base.html.twig' %}

{% block title %}Ressources{% endblock %}

{% block body %}
    <h1>Ressources</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ressource.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ressource.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ ressource.slug }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ ressource.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('webapp_ressources_index') }}">back to list</a>

    <a href="{{ path('webapp_ressources_edit', {'id': ressource.id}) }}">edit</a>

    {{ include('webapp_ressources/_delete_form.html.twig') }}
{% endblock %}
