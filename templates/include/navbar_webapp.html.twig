<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container{% if parameter.isBlocMenuFluid == 1 %}-fluid{% else %}{% endif %}">
        <a class="navbar-brand" href="{{ path('op_webapp_public_homepage')}}">
            {% if parameter.logoName %}
            <img class="img-fluid" src="{{ vich_uploader_asset(parameter, 'logoFile') }}" alt="{{ parameter.logoName }}" style="height: 45px;">
            {% else %}
            <img class="img-fluid" src="{{ asset('images/svg/user.svg') }}" alt="logo_OpenGaia" style="height: 45px;">
            {% endif %}
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('op_webapp_public_homepage')}}"> Accueil</a>
                </li>
                {% for menu in menus %}
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('op_webapp_page_slug', {'slug':menu.slug}) }}">{{ menu.name }}</a>
                </li>
                {% endfor %}
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('op_gestapp_adhesion_new2')}}"> Adhésion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('op_admin_dashboard_index')}}"><i class="fas fa-unlock-alt"></i> Mon espace</a>
                </li>
            </ul>
        </div>
    </div>
</nav>