<form method="post" action="{{ path('op_webapp_college_delete', {'id': college.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ college.id) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
