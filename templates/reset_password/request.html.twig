{% extends 'base.html.twig' %}

{% block title %}Réinitialisation "Mot de passe"{% endblock %}

{% block body %}
    <section class="container mt-5">
        <div class="row">
            <div class="col-12">
                {% for flashError in app.flashes('reset_password_error') %}
                    <div class="alert alert-danger" role="alert">{{ flashError }}</div>
                {% endfor %}
                <div class="mb-3">
                    <h1>Réinitialisation "Mot de passe"</h1>
                    <p>
                        Bonjour,<br>En accédant à cette page, vous allez pouvoir changer votre mot de passe de connexion à l'espace collège.<br>
                        Si vous ne connaissez pas ce dernier alors nous vous conseillons de contact le responsable jeuesse des Francas des Landes.
                    </p>
                </div>

                {{ form_start(requestForm) }}
                {{ form_row(requestForm.email) }}
                <div>
                    <small>
                        Entrez votre adresse mail et nous vous enverrons un lien pour le renouvellement.
                    </small>
                </div>

                <button class="btn btn-primary">Envoyer le lien</button>
                {{ form_end(requestForm) }}
            </div>
        </div>
    </section>

{% endblock %}
